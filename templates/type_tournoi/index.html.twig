{% extends 'base-back.html.twig' %}

{% block title %}TypeTournoi index{% endblock %}

{% block body %}

    <div class="container-fluid page-body-wrapper">
        <div class="main-panel">
            <div class="content-wrapper">
                <div class="row">
                    <div class="col-lg-6 grid-margin stretch-card">
                    </div>
                    <div class="col-lg-12 grid-margin stretch-card">
                    </div>
                    <div class="col-lg-12 stretch-card">
                        <div class="card">
                            <div class="card-body">
                                <h2 class="card-title">Table Tournoi</h2>

                            <a href="{{ path('type_tournoi_new') }}" class="btn btn-success btn-fw">Ajouter</a>


                            <table class="table table-bordered">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom_type</th>
                <th>Desc_type</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for type_tournoi in type_tournois %}
            <tr class="table-primary">
                <td>{{ type_tournoi.id }}</td>
                <td>{{ type_tournoi.nomType }}</td>
                <td>{{ type_tournoi.descType }}</td>
                <td>
                    <a class="btn btn-success btn-fw" href="{{ path('type_tournoi_edit', {'id': type_tournoi.id}) }}">Update</a>


                </td>
                <td>
                <div>{{ include('type_tournoi/_delete_form.html.twig') }}</div>
                </td>

            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

{% endblock %}
