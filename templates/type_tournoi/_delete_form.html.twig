<form method="post" action="{{ path('type_tournoi_delete', {'id': type_tournoi.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_tournoi.id) }}">
    <button  class="btn btn-danger btn-fw">Supprimer</button>
</form>
