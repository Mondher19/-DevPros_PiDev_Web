{% extends 'base-back.html.twig' %}

{% block title %}Hello CategorieController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>
<h1>Les catégories</h1>
        {% for i in categorie %}
            <tr>
        <td>
            <br>
            {{ i.sujet }}
            <br>

        </td>
                <td>
                    <a href="{{ path('deleteCategorie',{'id':i.id}) }} "  class="badge badge-outline-danger">supprimer</a>
                    <a href="{{ path('updateCategorie',{'id':i.id}) }} "  class="badge badge-outline-danger">modifier</a>

                </td>
            </tr>
        {% endfor %}




{% endblock %}
