<form method="post" action="{{ path('tournoi_delete', {'id_tour': tournoi.idtour}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tournoi.idtour) }}">
    <button class="btn btn-danger btn-fw">Supprimer</button>
</form>
