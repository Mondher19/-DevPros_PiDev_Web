{% extends 'base-back.html.twig' %}



{% block title %}  {% endblock %}

{% block body %}

    <div class="container">
    <div class="product_section_2" >

    <div class="row" >
        <div class="col-md-4">


            <p style="100px" > <input  id="myInput"  type="text" placeholder="search">

            <table class="table table-bordered" id="t"  width="100%" cellspacing="0">
                <thead>
        <tr>
            <td>Id</td>
            <td>Nom</td>
            <td>prenom</td>
            <td>mail</td>
            <td>Action</td>
            <td>action</td>

        </tr>
                </thead>
                <tbody>
                <tbody id="myTable">
        {% for i in user %}
            <tr>
                <td>{{ i.id }}</td>
                <td>{{ i.nom }}</td>
                <td>{{ i.prenom }}</td>
                <td>{{ i.mail }}</td>


                <td><a href="{{ path('userc_edit',{'id':i.id}) }}" >Update</a>
                </td>
                <td>
                    <a href="{{ path('removegamescat',{'id':i.id}) }}">Remove</a>
                    </td>

            </tr>
        {% endfor %}
                </tbody>
                </tbody>
                <tbody id="search">
                </tbody>


            </table>

            <br><br><br><br><br><br><br><br>
        </div></div>
        <a href="{{ path("app_logout") }}">logout</a>
    </div>
    </div>




    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script>
        $(document).ready(function(){
            $("#myInput").on("keyup", function() {
                var value = $(this).val().toLowerCase();
                $("#myTable tr").filter(function() {
                    $(this).toggle($(this).text().toLowerCase().indexOf(value) > -1)
                });
            });
        });
    </script>



{% endblock %}